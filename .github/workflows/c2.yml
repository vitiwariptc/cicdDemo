name: CI Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: SetUp Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.8'

      - name: Clone a Script Repo
        run: |
          git clone https://github.com/your-username/your-repo.git cloned-repo
          cd cloned-repo
          ls -la

      - name: Install Dependencies
        run: |
          cd cloned-repo
          python -m pip install --upgrade pip
          pip install -r cloned-repo/requirements.txt
            
      - name: Run Python File
        run: |
            cd cloned-repo
            python app.py
      